### Procedural macro for code comment exporting in `evestaticdata` crate

This macro is fairly poorly built, and just an excercise for Rust procedural macros.

Please don't include this in any default build if you fork this repo, it's not robust enough for that.